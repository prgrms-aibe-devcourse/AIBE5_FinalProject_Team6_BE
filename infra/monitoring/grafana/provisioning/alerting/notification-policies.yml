apiVersion: 1

policies:
  - orgId: 1
    receiver: fandrops-email
    group_by: [alertname, severity]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: fandrops-email
        matchers:
          - severity =~ "p0|p1|p2"
